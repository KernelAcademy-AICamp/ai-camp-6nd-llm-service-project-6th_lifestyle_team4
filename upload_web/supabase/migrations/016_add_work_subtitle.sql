-- ============================================================================
--  works.subtitle 컬럼 추가
--
--  배경:
--   - 셜록홈즈처럼 시리즈물(연작 소설·뮤지컬 시리즈·드라마 에피소드)에서는
--     "셜록홈즈" 같은 작품명을 title 로, "보헤미아 왕국의 스캔들" 같은
--     개별 편 이름을 subtitle 로 분리해 표시해야 자연스럽다.
--   - 기존엔 부제가 별도 컬럼 없이 title 에 통째로 들어가 PWA 카드에서
--     "<보헤미아 왕국의 스캔들>" 처럼 부제만 노출되거나, 긴 한 줄이
--     단어 중간에서 잘려 보이는 문제가 발생.
--
--  적용 방법 (Supabase Dashboard → SQL Editor):
--    아래 ALTER TABLE 문 실행.
-- ============================================================================

ALTER TABLE public.works
  ADD COLUMN IF NOT EXISTS subtitle varchar;

-- (선택) 기존 행을 손쉽게 갱신하려면 UPDATE 문을 따로 작성.
-- 예: 셜록홈즈 "보헤미아 왕국의 스캔들" 일화를 분리
-- UPDATE public.works
--    SET title    = '셜록홈즈',
--        subtitle = '보헤미아 왕국의 스캔들'
--  WHERE title = '보헤미아 왕국의 스캔들';
