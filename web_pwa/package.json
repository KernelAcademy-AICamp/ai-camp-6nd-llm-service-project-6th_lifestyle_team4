{
  "name": "pwa-web",
  "version": "0.1.0",
  "private": true,
  "description": "Daily Script — consumer PWA (오늘의 명대사)",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "deploy": "vercel deploy"
  }
}
